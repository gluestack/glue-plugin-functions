{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:dev": "tsc --project './tsconfig.json' --watch & nodemon NODE_ENV=development dist/",
    "start:server": "tsc && node dist",
    "lint": "./node_modules/.bin/tslint --fix -c tslint.json 'src/**/*.ts?(x)'"
  },
  "author": "Sandeep kumar",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.2",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "tslint": "^6.1.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "nodemon": "^2.0.20",
    "typescript": "^4.9.4"
  }
}
